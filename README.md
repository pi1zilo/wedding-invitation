# Свадебное приглашение — Айзирек & Таштемир

Интерактивное свадебное приглашение (27.09.2026). Мобильный дизайн 393×3981 px, масштабируется под любой экран.

## Структура проекта

```
├── index.html          ← главная (и единственная) страница
├── style.css           ← все стили
├── script.js           ← логика перехода и музыка
├── music/              ← фоновая музыка
│   └── music.mp3
├── fonts/              ← шрифты проекта
│   ├── roseversailles1.ttf
│   ├── DolomanPavljenko.otf
│   ├── Sensal Regular (RUS by Slavchansky)_0.ttf
│   └── Cruinn Thin.ttf
└── assets/             ← изображения (фоны, цветы, фото)
    ├── e3d6e603...      ← фоновое фото (постоянный фон)
    ├── 8765933c...      ← цветок (верхний, используется везде)
    ├── dae64a36...      ← цветок (нижний, используется везде)
    ├── 712009ee...      ← мраморная текстура (экран 3)
    ├── 144199ce...      ← декоративная картинка "Дорогие гости"
    ├── 121738b9...      ← декоративная картинка "Ждем Всех Вас"
    ├── 4199d186...      ← обручальные кольца
    └── b7468d1e...      ← фото ресторана
```

## Как запустить

Откройте `index.html` в браузере. Никакого сервера не нужно.

## Быстрое редактирование

| Что нужно изменить | Где смотреть |
|---------------------|--------------|
| Имена жениха/невесты | `index.html` — строка 43–45 |
| Дата свадьбы | `index.html` — строка 47 |
| Текст приветствия | `index.html` — строка 62–64 |
| Информация о ресторане | `index.html` — строка 76–78 |
| Фоновое изображение | `style.css` — `.persistent-bg` |
| Шрифты заголовков | `style.css` — `--font-rose` в `:root` |
| Шрифты обычного текста | `style.css` — `--font-doloman` в `:root` |
| Цвет текста | `style.css` — `--ink` в `:root` |
| Фоновые цвета | `style.css` — `:root` (переменные `--bg-*`) |
| Фоновая музыка | `music/music.mp3` — заменить файл |
| Фотографии | `assets/` — заменить файл, сохранив имя |

## Где что менять

### Тексты

| Что | Где |
|-----|-----|
| Имена жениха/невесты | `index.html` — строки 43–45 |
| Дата свадьбы | `index.html` — строка 47 |
| Приветствие гостям | `index.html` — строка 62 |
| Информация о ресторане | `index.html` — строка 76 |
| Надпись "Ждем Всех Вас" | `index.html` — строка 96 |

### Шрифты

Все шрифты подключены в `style.css` (раздел 1. FONTS).

| Шрифт | Файл | Где используется |
|-------|------|------------------|
| RoseVersailles1 | `fonts/roseversailles1.ttf` | Заголовки, кнопка, имена |
| Doloman | `fonts/DolomanPavljenko.otf` | Обычный текст, примечания |
| Sensal | `fonts/Sensal Regular...ttf` | Надпись "приглашаем на свадьбу" |
| Cruinn | `fonts/Cruinn Thin.ttf` | Дата |

### Цвета

Все цвета в CSS-переменных (`style.css`, раздел 2. CSS VARIABLES):

| Переменная | Значение | Назначение |
|------------|----------|------------|
| `--ink` | `#151515` | Основной цвет текста |
| `--bg-page` | `#111` | Фон страницы |
| `--bg-stage` | `#f7f7f5` | Фон stage |
| `--bg-white` | `#fff` | Белый фон |

### Изображения

Все изображения в папке `assets/`. Замените файл, сохранив имя (хеш).

### Размеры и расположение

Все размеры и позиции — в `style.css`, разделы 6–9. Каждый экран — отдельный блок.

### Анимации

| Что | Где |
|-----|-----|
| Crossfade переход | `script.js` — обработчик клика на `openInvite` |
| Время исчезновения | `script.js` — `FADE_OUT_MS` (600ms) |
| Время появления | `script.js` — `FADE_IN_MS` (800ms) |

### Музыка

| Что | Где |
|-----|-----|
| Файл музыки | `music/music.mp3` |
| Элемент audio | `index.html` — строка 103 |
| Запуск музыки | `script.js` — строка 48 |
| Зацикливание | `index.html` — атрибут `loop` |

## Адаптивность

Адаптивность реализована через CSS-переменную `--scale` и `transform: scale()` в `.design`. Медиа-запрос в конце `style.css` добавляет тень и отступ для экранов шире 393px.

## Порядок экранов

1. **Заставка "Нажми"** — кнопка и цветки на фоне
2. **Обложка** — имена, амперсанд, дата
3. **Основная информация** — приветствие, кольца, место/время
4. **Завершение** — "Ждем Всех Вас"

## Переход между экранами

При нажатии "Нажми":
1. Кнопка и цветки плавно исчезают (600ms)
2. Мгновенный переход к позиции второго экрана
3. Информация плавно проявляется (800ms)
4. Запускается фоновая музыка
5. Скролл разрешён

## Файлы, которые лучше не менять

- `assets/` — изображения (только заменять файлы, не переименовывать)
- `fonts/` — шрифты (только заменять файлы)
- `music/` — музыка (только заменять файл)
- Координаты и размеры в CSS — точная копия Figma-макета
